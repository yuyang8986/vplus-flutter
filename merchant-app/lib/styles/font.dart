import 'package:vplus_merchant_app/helpers/screenHelper.dart';
import 'package:vplus_merchant_app/helpers/sizeHelper.dart';

/// Here defines the font size, container border radius ratio and icon size
/// Suffix L for landscape, P for portrait.

const double largeScreenTitleFontSize = 25;
const double largeScreenTextBodyFontSize = 20;
const double largeScreenTextBodySmallFontSize = 15;
const double phoneScreenTitleFontSize = 50;
const double phoneScreenTextBodyFontSize = 40;
const double phoneScreenTextBodySmallFontSize = 30;
const double largeScreenTextFieldFontSize = 30;
const double phoneScreenTextFieldFontSize = 40;

// font size
final double kdsItemHeaderFontSize = SizeHelper.textMultiplier * 1.7;
final double kdsAddOnReceiptFontSize = SizeHelper.textMultiplier * 1.5;

final double largeVplusButtonTextSizeP = SizeHelper.textMultiplier * 2;
final double largeVplusButtonTextSizeL = SizeHelper.textMultiplier * 2;

final double shoppingCartAddOnReceiptTextSize = SizeHelper.textMultiplier * 2;

// widget size
final double customTextBoxIconSizeL = SizeHelper.imageSizeMultiplier * 3;
final double customTextBoxIconSizeP = SizeHelper.imageSizeMultiplier * 3.3;

// layout
// container size
final double kdsGridViewTileAspectRatio = SizeHelper.heightMultiplier * 0.10;
